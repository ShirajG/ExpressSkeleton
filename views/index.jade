block vars  
  -var pageTitle = 'Express Skeleton'

extends layout

append content
  .container
    if session && session.user
      p Welcome to the site user #{JSON.stringify(session.user.id)}
      p #{session.user.email}
    if flash
      p #{flash}